0. write unit tests
   a. Go table-driven tests
   b. tree.js tests and similar for the front end
1. recognize dates written in the item and add the DEADLINE property appropriately
   a. this includes explicit dates in a variety of formats (org standard, month-day-year, etc)
   b. this includes looser formats, like "tomorrow" or "next week"
   c. the date should be set at creation time but otherwise only changes explicitly
2. add ability to "zoom" so that the current item becomes the root in the view. Unzoom goes back to whole-doc.
3. add a concept of "bookmarks" that, when chosen, that item becomes the zoom root view.
4. add a "priority" concept like that in true Org mode.
   a. the priority is like a badge which can cycle
   b. it uses org mode priority label in the file
   c. the priority colors (red, yellow, blue) apply to the whole item title
   
   
